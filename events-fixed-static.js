/* EVENTS FIXED STATIC — DISABLED
 * The main application runtime now owns Vi phạm/Khen thưởng rendering and CRUD.
 * Kept as a no-op to prevent legacy save handlers from competing with Master CRUD.
 */
(function(){'use strict';window.__LH_EVENTS_FIXED_STATIC_DISABLED__=true;})();
