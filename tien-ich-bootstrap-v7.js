/* TIỆN ÍCH BOOTSTRAP V7
   Luôn bảo đảm menu Tiện ích tồn tại, không phụ thuộc việc V5/V6 đã nạp hay chưa.
*/
(function(){'use strict';
if(window.__LH_UTILITY_BOOTSTRAP_V7__)return;window.__LH_UTILITY_BOOTSTRAP_V7__=true;
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
const clean=v=>String(v??'').trim();
function students(){try{if(typeof window.getStudentsSafe==='function'){const a=window.getStudentsSafe();if(Array.isArray(a)&&a.length)return a.filter(x=>x&&x.id&&x.name)}}catch(e){}return Array.isArray(window.students)?window.students.filter(x=>x&&x.id&&x.name):[]}
function records(){for(const k of ['learningRecords','hocTapRecords','hocTapData','learningData'])if(Array.isArray(window[k]))return window[k];return[]}
function level(s){const d=s?.xepLoai||s?.xepLoaiHocTap||s?.academicLevel||s?.learningLevel||s?.classification||s?.hocLuc||s?.trinhDo||s?.trinhDoHocTap||s?.mucDo;if(clean(d))return clean(d);const a=records().filter(r=>String(r?.studentId||'')===String(s?.id||''));for(let i=a.length-1;i>=0;i--){const v=a[i]?.level||a[i]?.xepLoai||a[i]?.result;if(clean(v))return clean(v)}return'Chưa xác định'}
function ensureMenu(){const nav=document.querySelector('.main-menu');if(!nav)return;let item=nav.querySelector('[data-page="utilities"]');if(!item){item=document.createElement('button');item.type='button';item.className='menu-item';item.dataset.page='utilities';item.innerHTML='<i class="fa-solid fa-toolbox"></i><span>Tiện ích</span>';const settings=nav.querySelector('[data-page="settings"]');if(settings)nav.insertBefore(item,settings);else nav.appendChild(item)}item.onclick=function(){openPage()}}
function openPage(){document.querySelectorAll('.page-section').forEach(x=>x.classList.remove('active'));let p=$('page-utilities');if(!p){p=document.createElement('section');p.className='page-section';p.id='page-utilities';p.dataset.pageSection='utilities';$('mainContent')?.appendChild(p)}p.classList.add('active');document.querySelectorAll('.main-menu .menu-item').forEach(x=>x.classList.remove('active'));document.querySelector('.main-menu [data-page="utilities"]')?.classList.add('active');const title=$('pageTitle');if(title)title.textContent='Tiện ích';render(p)}
function render(p){p.innerHTML='<div class="page-header"><div><span class="page-eyebrow">🧰 Công cụ giáo viên</span><h1>Tiện ích</h1><p>Công cụ tổ chức lớp học theo dữ liệu thực tế.</p></div></div><div class="dashboard-panel"><div class="panel-header"><div><h3>🧰 Tiện ích lớp học</h3><p><strong>2 học sinh/bàn</strong> · <strong>Mỗi dãy bàn = 1 tổ</strong>.</p></div></div><div class="quick-actions"><button type="button" class="quick-action" id="lhDownloadLevelsV7"><span class="quick-action-icon">📥</span><span><strong>Tải danh sách trình độ HS</strong><small>TT · Họ tên HS · Xếp loại thực tế · CSV</small></span></button><button type="button" class="quick-action" id="lhSeatV7"><span class="quick-action-icon">📐</span><span><strong>Tạo sơ đồ lớp học</strong><small>2 HS/bàn · mỗi dãy là 1 tổ · ngẫu nhiên</small></span></button></div><div id="lhUtilityWorkV7"></div></div>';
$('lhDownloadLevelsV7').onclick=downloadLevels;$('lhSeatV7').onclick=seat}
function downloadLevels(){const a=students();if(!a.length){alert('Chưa có danh sách học sinh để tải.');return}const rows=[['TT','Họ tên HS','Xếp loại'],...a.map((s,i)=>[i+1,s.name,level(s)])];const csv='\uFEFF'+rows.map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\r\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8;'}),url=URL.createObjectURL(blob),ael=document.createElement('a');ael.href=url;ael.download='DANH_SACH_TRINH_DO_HS.csv';document.body.appendChild(ael);ael.click();ael.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
function norm(v){return clean(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/đ/g,'d').replace(/Đ/g,'D').toLowerCase()}
function rank(v){const n=norm(v);if(/xuat sac|gioi|tot|hoan thanh tot/.test(n))return 3;if(/kha|dat|hoan thanh/.test(n))return 2;if(/trung binh|yeu|kem|chua dat/.test(n))return 1;return 0}
function shuffle(a){a=[...a];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
function seat(){const a=students();if(!a.length){alert('Chưa có danh sách học sinh.');return}let n=Number(prompt('Số dãy bàn (mỗi dãy = 1 tổ):','4'));if(!Number.isInteger(n)||n<1)n=4;n=Math.min(n,Math.ceil(a.length/2));const pool=shuffle(a),pairs=[];while(pool.length){const x=pool.shift();let i=pool.findIndex(y=>rank(level(y))!==rank(level(x)));if(i<0)i=0;const y=pool.length?pool.splice(i,1)[0]:null;pairs.push(y?[x,y]:[x])}shuffle(pairs);const g=Array.from({length:n},()=>[]);pairs.forEach((p,i)=>g[i%n].push(p));const max=Math.max(...g.map(x=>x.length));let rows='';for(let r=0;r<max;r++){rows+='<tr><td><strong>Bàn '+(r+1)+'</strong></td>';for(let c=0;c<n;c++){const p=g[c][r];rows+='<td>'+(p?p.map(s=>'<div><strong>'+esc(s.name)+'</strong><small> ('+esc(level(s))+')</small></div>').join(''):'—')+'</td>}rows+='</tr>'}const w=$('lhUtilityWorkV7');w.innerHTML='<div class="panel-header"><div><h3>📐 Sơ đồ lớp học</h3><p>'+a.length+' học sinh · '+pairs.length+' bàn · '+n+' dãy/tổ · 2 HS/bàn khi đủ dữ liệu.</p></div></div><div class="table-container"><table class="data-table"><thead><tr><th>Bàn</th>'+Array.from({length:n},(_,i)=>'<th>Dãy '+(i+1)+' — Tổ '+(i+1)+'</th>').join('')+'</tr></thead><tbody>'+rows+'</tbody></table></div>'}
function install(){ensureMenu();window.addEventListener('load',ensureMenu);setTimeout(ensureMenu,500)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();