/* LE HOANG EVENT SYNC V21.1 — multi-student + authoritative Sheets delete */
(function(){
  'use strict';
  if(window.__LH_EVENT_SYNC_V211__) return;
  window.__LH_EVENT_SYNC_V211__=true;
  const bridge=()=>window.LH_GOOGLE_SHEETS_V20||null;
  const clean=v=>String(v??'').trim().replace(/\s+/g,' ');
  function selectedIds(id){const el=document.getElementById(id);if(!el)return[];if(el.multiple)return Array.from(el.selectedOptions||[]).map(o=>clean(o.value)).filter(Boolean);const v=clean(el.value);return v?[v]:[]}
  function makeMulti(id){const el=document.getElementById(id);if(el){el.multiple=true;el.size=Math.min(Math.max(el.options.length,4),8);el.title='Có thể chọn nhiều học sinh'}}
  function wrapSave(name,tab){const original=window[name];if(typeof original!=='function'||original.__lhWrappedV211__)return;const wrapped=function(data){const result=original.apply(this,arguments);return Promise.resolve(result).then(async r=>{if(r===false||(r&&r.success===false)||!r)return r;const b=bridge();if(!b?.save)return r;try{return {...r,googleSheets:await b.save(tab,r.record||data||{})}}catch(e){console.error('[V21.1] save',tab,e);if(typeof window.showToast==='function')window.showToast('Đã lưu cục bộ nhưng chưa đồng bộ Google Sheets: '+e.message,'error');return r}})};wrapped.__lhWrappedV211__=true;window[name]=wrapped}
  function wrapDelete(name,tab,getRecord){const original=window[name];if(typeof original!=='function'||original.__lhWrappedV211__)return;const wrapped=async function(id){const old=getRecord?getRecord(id):null;const b=bridge();if(b?.delete){try{await b.delete(tab,id,old)}catch(e){console.error('[V21.1] delete blocked',tab,e);if(typeof window.showToast==='function')window.showToast('Google Sheets chưa xóa được bản ghi. Dữ liệu giao diện được giữ nguyên.','error');return false}}const local=original.apply(this,arguments);return local===undefined?true:local};wrapped.__lhWrappedV211__=true;window[name]=wrapped}
  function installSelects(){['violationStudent','rewardStudent'].forEach(makeMulti);if(typeof window.updateStudentSelects==='function'&&!window.updateStudentSelects.__lhWrappedV211__){const original=window.updateStudentSelects;const wrapped=function(){const old={};['violationStudent','rewardStudent'].forEach(id=>{const el=document.getElementById(id);if(el)old[id]=el.multiple?Array.from(el.selectedOptions||[]).map(o=>o.value):[el.value]});const r=original.apply(this,arguments);['violationStudent','rewardStudent'].forEach(id=>{const el=document.getElementById(id);if(!el)return;el.multiple=true;el.size=Math.min(Math.max(el.options.length,4),8);const vals=new Set((old[id]||[]).filter(Boolean));if(vals.size)Array.from(el.options).forEach(o=>o.selected=vals.has(o.value))});return r};wrapped.__lhWrappedV211__=true;window.updateStudentSelects=wrapped}}
  async function saveMany(fn,ids,build){if(!ids.length){if(typeof window.showToast==='function')window.showToast('Vui lòng chọn ít nhất một học sinh.','warning');return false}let ok=0;for(const id of ids){try{const r=await window[fn](build(id));if(r!==false&&!(r&&r.success===false))ok++}catch(e){console.error('[V21.1] batch',fn,e)}}if(typeof window.refreshAll==='function')window.refreshAll();if(typeof window.showToast==='function')window.showToast(`Đã lưu ${ok}/${ids.length} học sinh lên hệ thống.`,ok===ids.length?'success':'warning');return ok===ids.length}
  document.addEventListener('submit',async function(e){const form=e.target;if(form?.id==='violationForm'){e.preventDefault();e.stopImmediatePropagation();const ids=selectedIds('violationStudent');await saveMany('addViolation',ids,id=>({studentId:id,date:document.getElementById('violationDate')?.value||new Date().toISOString().slice(0,10),type:document.getElementById('violationType')?.value||'',level:document.getElementById('violationLevel')?.value||'',note:document.getElementById('violationNote')?.value||document.getElementById('violationDescription')?.value||''}));if(typeof window.closeModal==='function')window.closeModal('violationModal');return}if(form?.id==='rewardForm'){e.preventDefault();e.stopImmediatePropagation();const ids=selectedIds('rewardStudent');await saveMany('addReward',ids,id=>({studentId:id,date:document.getElementById('rewardDate')?.value||new Date().toISOString().slice(0,10),type:document.getElementById('rewardType')?.value||'',formType:document.getElementById('rewardFormType')?.value||'',note:document.getElementById('rewardNote')?.value||''}));if(typeof window.closeModal==='function')window.closeModal('rewardModal');return}},true);
  window.__LH_V21_INSTALL__=function(){installSelects();wrapSave('saveAttendanceRecord','DIEM_DANH');wrapSave('addViolation','VI_PHAM');wrapSave('addReward','KHEN_THUONG');wrapDelete('deleteViolation','VI_PHAM',id=>(window.violationRecords||[]).find(x=>clean(x?.id)===clean(id)));wrapDelete('deleteReward','KHEN_THUONG',id=>(window.rewardRecords||[]).find(x=>clean(x?.id)===clean(id)))};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(window.__LH_V21_INSTALL__,0),{once:true});else setTimeout(window.__LH_V21_INSTALL__,0);
})();
