/* MENU RUNTIME 11.0 — DISABLED
 * script.js + data.js are the only runtime core for the main application.
 * Optional repair/compatibility modules are disabled here while the touch
 * interaction regression is isolated. This loader registers no listeners,
 * observers, timers, DOM mutations, or navigation handlers.
 */
(function(){
  'use strict';
  if(window.__MENU_RUNTIME_FIX_1100__) return;
  window.__MENU_RUNTIME_FIX_1100__=true;
  window.__LH_OPTIONAL_RUNTIME_DISABLED__=true;
})();
