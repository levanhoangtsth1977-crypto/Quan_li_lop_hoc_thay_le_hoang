name: Obsolete preboot router guard

on:
  workflow_dispatch:

# Kept as an archived manual record only.
# The application now has one canonical index.html source and must not be
# rewritten automatically by CI after every push.
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived: no automatic code mutation."
