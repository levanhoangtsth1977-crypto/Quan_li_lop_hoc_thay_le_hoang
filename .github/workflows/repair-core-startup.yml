name: Obsolete core startup repair guard

on:
  workflow_dispatch:

# Archived manual workflow. Production code is now maintained directly in
# the canonical application files; CI must not rewrite them after pushes.
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived: no automatic code mutation."
