/* DATA SYNC VIETNAMESE FIX — RETIRED
 * This legacy module contained a one-time remote cleanup routine and a second
 * data/report renderer. Automatic deletion of Google Sheets event data must
 * never run from the frontend. Data synchronization is owned by
 * google-api-bridge.js + the canonical Master CRUD layer.
 */
(function(){'use strict';window.__LH_DATA_SYNC_VIETNAMESE_FIX_RETIRED__=true;})();
