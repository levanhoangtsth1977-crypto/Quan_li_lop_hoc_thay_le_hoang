/* VI PHẠM STALE-UI CLEANER — RETIRED
 * The canonical renderViolations() in script.js owns the table.
 * This legacy MutationObserver could erase freshly rendered rows while the
 * Google data array was temporarily empty. It is intentionally inert.
 */
(function(){'use strict';window.__LH_VIOLATION_STALE_UI_CLEANER_RETIRED__=true;})();
