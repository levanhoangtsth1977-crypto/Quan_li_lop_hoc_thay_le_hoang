/* TRIỆU PHÚ HỌC ĐƯỜNG — HELP CONTROLS
   Các nút trợ giúp được xử lý trực tiếp bởi game.js 4.0.
   File này giữ lại để không thay đổi cấu trúc index.html và không ghi đè event handler. */
(function(){'use strict';
  window.addEventListener('questionBankReady',function(){
    /* game.js tự đồng bộ và gắn lại 3 nút: 50/50, Hỏi cả lớp, Đổi câu. */
  });
})();