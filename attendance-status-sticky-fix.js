/* ATTENDANCE STATUS STICKY — DISABLED
   The native select is now stable without MutationObserver or pointer interception.
   Persistence is handled by the attendance data engine when the teacher saves.
*/
(function(){
  'use strict';
  window.__LH_ATTENDANCE_STATUS_STICKY_DISABLED__=true;
})();
