/* EVENT SUMMARY BY STUDENT FIX 3.1 — 1 HS/1 DÒNG + THỐNG KÊ 3 NÚT */
(function(){
'use strict';
if(window.__LH_EVENT_SUMMARY_BY_STUDENT_31__)return;
window.__LH_EVENT_SUMMARY_BY_STUDENT_31__=true;
const $=id=>document.getElementById(id),clean=v=>String(v??'').trim(),norm=v=>clean(v).toLocaleLowerCase('vi'),esc=v=>clean(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
function records(name,getter){try{if(typeof window[getter]==='function'){const a=window[getter]();if(Array.isArray(a))return a.slice()}}catch(_){}return Array.isArray(window[name])?window[name].slice():[]}
function students(){try{if(typeof window.getStudentsSafe==='function'){const a=window.getStudentsSafe();if(Array.isArray(a))return a}}catch(_){}return Array.isArray(window.students)?window.students:[]}
function studentName(id){const sid=clean(id);try{if(typeof window.getStudentById==='function'){const s=window.getStudentById(sid);if(s?.name)return s.name}}catch(_){}return students().find(s=>clean(s.id)===sid)?.name||'Học sinh'}
function date(v){const s=clean(v);if(/^\d{4}-\d{2}-\d{2}$/.test(s)){const [y,m,d]=s.split('-');return `${d}/${m}/${y}`}return s}
function group(list){const m=new Map();list.forEach(r=>{const id=clean(r?.studentId);if(!id)return;if(!m.has(id))m.set(id,[]);m.get(id).push(r)});return [...m.entries()].map(([studentId,items])=>({studentId,name:studentName(studentId),items:items.sort((a,b)=>clean(b.date).localeCompare(clean(a.date)))})).sort((a,b)=>a.name.localeCompare(b.name,'vi'))}
function ensureBox(){const page=$('page-statistics');if(!page)return null;let box=$('lhEventStatistics');if(!box){box=document.createElement('div');box.id='lhEventStatistics';box.className='dashboard-panel';const grid=$('statisticsGrid');if(grid)grid.replaceWith(box);else page.appendChild(box)}return box}
function renderStats(kind){const box=ensureBox();if(!box)return;const data={attendance:records('attendanceRecords','getAttendanceRecords').filter(r=>['absent','excused','vắng','có phép','co phep'].includes(norm(r.status))),violation:records('violationRecords','getViolationRecords'),reward:records('rewardRecords','getRewardRecords')};const rows=group(data[kind]);let title='',icon='',head='',empty='';if(kind==='attendance'){title='HS vắng';icon='📌';head='<th>Tổng vắng</th><th>Có phép</th><th>Không phép</th><th>Ngày vắng</th>';empty='Chưa có học sinh vắng'}else if(kind==='violation'){title='HS vi phạm';icon='⚠️';head='<th>Số lần</th><th>Ngày / Nội dung / Mức độ</th><th>Thao tác</th>';empty='Chưa có học sinh vi phạm'}else{title='HS khen thưởng';icon='🏆';head='<th>Số lần</th><th>Ngày / Thành tích / Hình thức</th><th>Thao tác</th>';empty='Chưa có học sinh được khen thưởng'}let body='';if(!rows.length)body=`<tr><td colspan="8"><div class="empty-state"><strong>${empty}</strong><p>Dữ liệu sẽ tự động tổng hợp theo từng học sinh.</p></div></td></tr>`;else body=rows.map((r,i)=>{if(kind==='attendance'){const exc=r.items.filter(x=>['excused','có phép','co phep'].includes(norm(x.status))).length;return `<tr><td>${i+1}</td><td><strong>${esc(r.name)}</strong></td><td><strong>${r.items.length}</strong></td><td>${exc}</td><td>${r.items.length-exc}</td><td>${esc([...new Set(r.items.map(x=>date(x.date)))].join(', '))}</td></tr>`}const details=r.items.map(x=>`<div class="lh-event-detail"><strong>${esc(date(x.date))}</strong> — ${esc(x.type||x.note||'Chưa ghi')} — ${esc(x.level||x.formType||'Chưa ghi')}</div>`).join('');const dels=r.items.map(x=>`<button type="button" class="icon-button danger" title="Xóa đúng lượt ${esc(date(x.date))}" data-lh-delete-kind="${kind}" data-lh-delete-id="${esc(x.id)}"><i class="fa-solid fa-trash"></i></button>`).join(' ');return `<tr><td>${i+1}</td><td><strong>${esc(r.name)}</strong></td><td><strong>${r.items.length}</strong></td><td>${details}</td><td>${dels}</td></tr>`}).join('');box.innerHTML=`<div class="panel-header"><div><h3>${icon} ${title}</h3><p>1 học sinh = 1 dòng · ${rows.length} học sinh</p></div><div class="lh-stat-buttons"><button type="button" class="button secondary" data-lh-stat-kind="attendance">📌 HS vắng</button> <button type="button" class="button secondary" data-lh-stat-kind="violation">⚠️ HS vi phạm</button> <button type="button" class="button secondary" data-lh-stat-kind="reward">🏆 HS khen thưởng</button></div></div><div class="table-container"><table class="data-table"><thead><tr><th>STT</th><th>Học sinh</th>${head}</tr></thead><tbody>${body}</tbody></table></div>`}
function renderAll(){renderStats(window.__LH_STAT_KIND__||'attendance')}
async function deleteOne(kind,id){if(kind==='attendance')return;const fn=kind==='violation'?window.deleteViolation:window.deleteReward;if(typeof fn!=='function'){if(typeof window.showToast==='function')window.showToast('Chưa có API xóa dữ liệu.','error');return}if(!window.confirm('Xóa đúng 1 lượt này?\nCác lượt khác của học sinh vẫn giữ nguyên.'))return;const ok=await fn(id);if(ok===false){if(typeof window.showToast==='function')window.showToast('Không thể xóa lượt.','error');return}if(typeof window.syncAppDataReferences==='function')window.syncAppDataReferences();renderAll();if(typeof window.updateBadges==='function')window.updateBadges();if(typeof window.renderDashboard==='function')window.renderDashboard()}
document.addEventListener('click',e=>{const b=e.target.closest?.('[data-lh-stat-kind]');if(b){e.preventDefault();window.__LH_STAT_KIND__=b.dataset.lhStatKind;renderStats(window.__LH_STAT_KIND__);return}const d=e.target.closest?.('[data-lh-delete-id]');if(d){e.preventDefault();deleteOne(d.dataset.lhDeleteKind,d.dataset.lhDeleteId)}},true);
function install(){try{if(typeof PAGE_RENDERERS!=='undefined'){PAGE_RENDERERS.violations=window.renderViolations;PAGE_RENDERERS.rewards=window.renderRewards}}catch(_){}renderAll()}
window.__LH_EVENT_SUMMARY_API__={renderAbsenceSummary:()=>renderStats('attendance'),renderGroupedViolations:()=>renderStats('violation'),renderGroupedRewards:()=>renderStats('reward'),refreshAll:renderAll};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();
