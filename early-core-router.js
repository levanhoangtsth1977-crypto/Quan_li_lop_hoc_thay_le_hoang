/* EARLY CORE ROUTER 2.0 — DISABLED
 * Canonical UI events are owned by script.js.
 * This file remains only so the existing index.html reference stays valid.
 * It intentionally registers NO event listeners and performs NO navigation.
 */
(function(){
  'use strict';
  window.__LH_EARLY_CORE_ROUTER_DISABLED__=true;
})();
